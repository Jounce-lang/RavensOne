// Minimal test case to reproduce the __else_block bug
// Expected: Should compile successfully
// Actual: Borrow checker error: undefined variable '__else_block'

fn test() -> i32 {
    if true {
        1
    } else {
        jnc
    }
}

fn main() {
    let result = test();
    println!("Result: {}", result);
}
