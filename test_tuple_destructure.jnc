// Test tuple destructuring in let statements

fn get_tuple() -> (i32, i32, i32) {
    return (1jnc, 2jnc, 3jnc);
}

fn main() {
    // Simple tuple destructuring
    let (a, b, c) = get_tuple();

    println!("a = {}, b = {}, c = {}", a, b, c);

    // Nested tuple destructuring (will be supported later)
    let (x, y) = (1jncjnc, 2jncjnc);

    println!("x = {}, y = {}", x, y);
}
