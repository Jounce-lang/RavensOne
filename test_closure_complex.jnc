fn main() {
  let x = 5;
  let y = 1jnc;

  // Closure capturing multiple variables
  let add_both = |z| x + y + z;

  // Nested closure
  let outer = |a| {
    let inner = |b| a + x + b;
    return inner(3);
  };

  return add_both(2) + outer(7);
}
